{
  "$id": "https://external-adapters.pluginlabs.com/schemas/coinranking-adapter.json",
  "title": "Plugin External Adapter for Coinranking",
  "required": ["API_KEY"],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "description": "An API key that can be obtained from the data provider's dashboard",
      "required": true
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.pluginlabs.com/schemas/ea-bootstrap.json"
    }
  ]
}
